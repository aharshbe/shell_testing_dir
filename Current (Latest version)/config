# Configuration file
# Generally 1 means 'turn feature on', 0 means 'turn feature off'

# -- ATTENTION: Please change below --

#TO CHANGE: 
#SHELL should be your shell's directory and TSHELL is the shell to test #agains

SHELL="REPLACE ME WITH THE PATH TO YOUR SHELL"

	#example: SHELL="../simple_shell/h"

LSDIR="REPLACE WITH PATH TO DIR WHICH HOLDS YOUR SHELL"

	#exmaple: LSDIR="./HolbertonRepos/bash_stuff/simple_shell"

bettycheckerdir="SAME AS LASDIR"

	#example: bettycheckerdir="./HolbertonRepos/bash_stuff/simple_shell"
	
TSHELL="/bin/sh"

#Optional settings

FUNMODE=0
SHOWEGG=1
COUNTADV=1
SHOWERRORS=0
valgrind_check=1
betty_check=1

#-- WARNING --
#Please don't modify these!!!

TESTDIR="."
R=$RANDOM
OUTPUTFILE="checker_output_$R"
ERROROUTPUTFILE="checker_error_output_$R"
SLEEP=`which sleep`
SLEEPSECONDS=0.3
LTRACE=`which ltrace`
LTRACEOUTPUTFILE="checker_ltrace_output_$R"
ECHO=`which echo`
CAT=`which cat`
GREP=`which grep`
WC=`which wc`
RM=`which rm`
TOUCH=`which touch`
CHMOD=`which chmod`
